include_directories(${CMAKE_CURRENT_SOURCE_DIR} ${CMAKE_CURRENT_BINARY_DIR})

set(_CBFOREST_VENDOR_BUILD_SHARED_LIBS ${BUILD_SHARED_LIBS})
set(BUILD_SHARED_LIBS ON)

add_subdirectory(snappy)
add_subdirectory(forestdb)
#add_subdirectory(sqlite-unicodesn)

set(BUILD_SHARED_LIBS {${_CBFOREST_VENDOR_BUILD_SHARED_LIBS})
unset(_CBFOREST_VENDOR_BUILD_SHARED_LIBS)

set(FORESTDB_INCLUDE_DIRS ${FORESTDB_INCLUDE_DIRS} PARENT_SCOPE)
